# ROFAZ ACADEMY — MASTER OMR TEST MATRIX

This matrix enumerates all components of the physical OMR pipeline, establishing strict testing criteria and verification methods across Unit, Integration, End-to-End, Real-Life, Failure, Security, and Performance dimensions.

---

## Complete Test Matrix

| # | Component | Input | Expected Output | Unit Test | Integration Test | E2E Test | Real-Life Test | Failure Test | Security Test | Performance Test | Status |
|---|---|---|---|---|---|---|---|---|---|---|---|
| **1** | **Template Generator & Schema Validator** | Config (dimensions, grid count, QR position) | Valid `GeometryTemplate` with non-overlapping ROIs | `GeometryTemplate.test` | Template → Marker mapping | Full sheet render | Laser & Inkjet print | Overlapping / negative coordinates | Schema tampering / injection | Generation < 5ms | **VERIFIED** |
| **2** | **Image Quality Engine** | Raw camera frame / image buffer | `QualityMetrics` (blur, luminance, contrast, pass/fail) | `QualityEngine.test` | Quality → Auto-capture gating | Live video stream check | Low light, heavy glare, blur | Blank / completely dark frame | Oversized payload / memory bomb | Eval < 8ms/frame | **VERIFIED** |
| **3** | **Fiducial Marker Detector** | Captured frame with 4 corner targets | `CornerQuad` (TL, TR, BR, BL coordinates) | `MarkerDetector.test` | Marker → Warp integration | Camera auto-track quad | 0°–20° tilt, skewed angles | Missing 1+ corner marker | Fake markers / noisy borders | Detection < 15ms | **VERIFIED** |
| **4** | **Perspective Warp Engine** | Image buffer + CornerQuad | Canonical 2480×3508 unwarped buffer | `PerspectiveWarp.test` | Warp → ROI extraction | Full unwarp from stream | Wrinkled / curved paper | Degenerate collinear quad | Coordinate overflow | Warp < 25ms | **VERIFIED** |
| **5** | **QR Decoder & Parser** | Top-right QR bounding box | Structured `QRContext` (examId, examSetId, classId) | `QRDecoder.test` | QR → ExamSet lookup | Camera scan to exam context | Low contrast, smudged print | Blurred / obstructed QR | Tampered examId / expired token | Decode < 10ms | **VERIFIED** |
| **6** | **Bubble Ink Classifier** | 100 question bubble ROIs (A/B/C/D) | `BubbleAnalysisResult` (score, fillRatio, decision) | `BubbleClassifier.test`| Classifier → Question matrix | Full sheet 100-Q scoring | Pencil / pen / light / tick / cross | Empty bubble / double mark | Image buffer overflow | 400 bubbles < 15ms | **VERIFIED** |
| **7** | **Roll Number Reader** | 6-column × 10-row bubble matrix | 6-digit Roll string (e.g. "307418") + status | `DigitReader.test` | Roll → Student DB lookup | Camera scan to student record | `000000`, `999999`, random digits | Missing digit / double fill | Injected alphanumeric string | Matrix read < 5ms | **VERIFIED** |
| **8** | **Registration Reader** | 7-column × 10-row bubble matrix | 7-digit Reg string (e.g. "7890123") + status | `DigitReader.test` | Reg → Student DB lookup | Camera scan to student record | High school reg numbers | Ambiguous / multiple marks | Cross-student manipulation | Matrix read < 5ms | **VERIFIED** |
| **9** | **Student Identity Resolver** | QR + Roll + Registration | `ResolvedStudentIdentity` (studentId, name, class) | `IdentityResolver.test`| Resolver → DB student record | Live scan to student match | Single & multi-section batches | Roll not in class / unknown student| Student ID impersonation | Lookup < 10ms | **VERIFIED** |
| **10**| **ExamSet Resolver** | QR `examSetId` or Set Bubble | `CanonicalQuestionSet` with stable question IDs | `ExamSetResolver.test` | Resolver → `ExamSet.questionsJson` | Live scan to question map | Sets A, B, C, D randomized | Corrupted question JSON | Unauthorized exam set access | Resolution < 5ms | **VERIFIED** |
| **11**| **Physical Response Mapper**| Physical answers + `CanonicalQuestionSet` | Normalized canonical answers matching DB IDs | `ResponseMapper.test` | Mapper → Submission payload | Complete sheet mapped to DB | Sequential & non-sequential IDs | Out-of-bounds question numbers | Manipulated option values | Map 100 Qs < 2ms | **VERIFIED** |
| **12**| **OMR Submission Adapter** | Scan result + Identity + Mapped answers | `CanonicalExamSubmissionPayload` (same as online)| `SubmissionAdapter.test`| Adapter → `evaluateSubmission()` | End-to-end scan submission | Mixed MCQ, MMCQ, AR formats | Identity / Exam mismatch | Malformed submission injection | Adapt < 5ms | **VERIFIED** |
| **13**| **Server Evaluation Engine**| Canonical submission payload | Calculated scores, negative marks, grades | `Evaluator.test` | Adapter → `evaluateSubmission` | Submit API → Postgres Result | Negative marking penalty cases | Divide-by-zero on 0-mark exams | Client-supplied score bypass | Eval 100 Qs < 10ms | **VERIFIED** |
| **14**| **Result Persistence Writer**| Evaluated score + Grade scale | Postgres `Result`, `ExamSubmission`, `OMRScan` | `ResultWriter.test` | DB transaction atomicity | Result portal `/exams/results/[id]`| High volume batch write | DB connection drop during write | SQL injection via comment/meta | Write < 20ms | **VERIFIED** |
| **15**| **Offline Outbox & Storage** | Scanned sheets when network is disconnected | IndexedDB persistence (`omr_scans_outbox`) | `ScanOutbox.test` | Outbox → Local cache | Offline scan survival on reload | Network dropped during scan | IndexedDB quota exceeded | Local storage tampering | Store < 5ms | **VERIFIED** |
| **16**| **Sync & Idempotency Manager**| Pending outbox scans upon reconnection | Successful sync with 0 duplicate DB records | `SyncManager.test` | Sync → Submit API | Background auto-sync | Flaky / slow network (3G) | Duplicate retry after lost ACK | Replay attack with stale UUID | Sync batch < 500ms | **VERIFIED** |
| **17**| **Teacher Review Console** | Scans flagged as `REVIEW_REQUIRED` | Teacher inspection UI with side-by-side paper | `ReviewConsole.test` | Review → Manual correction | Correct answer & update DB | Ambiguous bubble resolutions | Invalid correction overrides | Unauthorized teacher review | UI render < 50ms | **VERIFIED** |
| **18**| **Online/OMR Isomorphism** | Same student answers via Web vs OMR sheet | Identical score, grades, and diagnostics in DB | `Isomorphism.test` | Online vs OMR comparison | Golden test suite verification | Mixed correct/wrong/skipped | Differing question permutations | Tampered answer structures | Comparison < 5ms | **VERIFIED** |
| **19**| **Student Dashboard View** | Student viewing `/exams/results/[id]` | Full diagnostic breakdown, explanations, source | `DashboardView.test` | DB Result → React Component | Real student portal access | Mobile & desktop browsers | Unreleased / draft results | IDOR on other student results | Page load < 200ms | **VERIFIED** |
| **20**| **Batch Scanning HUD** | 50–500 continuous sheets scanned | Real-time counter, exception queue, zero leak | `BatchScan.test` | Continuous scan stream | 100 continuous paper scans | Variable paper orientations | Intermittent corrupted sheet | Memory leak over 500 frames | FPS >= 30, Memory stable | **VERIFIED** |
